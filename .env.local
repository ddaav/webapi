# Database Configuration
# Replace this with your actual MongoDB connection string (e.g., MongoDB Atlas URI or local connection URI)
MONGODB_URI=mongodb://127.0.0.1:27017/ghar-purja

# JWT Configuration
# Change this to a secure random secret key in production environment
JWT_SECRET=your_super_secret_jwt_key_please_change_me_in_production
